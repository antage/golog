/*
The golog package implements logging to multiple backends.
Each backend can filter messages by severity mask and have custom formatter.
The package includes io.Writer, console and syslog backends.
*/
package golog
